{
  "name": "ticket-desk",
  "version": "1.0.0",
  "private": true,
  "description": "Flask ticketing application with a mockable USU SM service layer.",
  "scripts": {
    "postinstall": "python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install -r requirements.txt",
    "start": ".venv/bin/flask --app app run --host 127.0.0.1 --port 5000",
    "start:lan": ".venv/bin/flask --app app run --host 0.0.0.0 --port 5000",
    "check": ".venv/bin/python -m compileall app.py usu_sm_service.py"
  },
  "keywords": [
    "flask",
    "tickets",
    "usu-sm"
  ],
  "license": "UNLICENSED"
}
